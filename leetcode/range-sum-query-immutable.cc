#include <vector>

using namespace std;

class NumArray {
private:
  vector<vector<int>> sum_range;

public:
  NumArray(vector<int> &nums) {

  }

  int sumRange(int i, int j) {

  }
};


// Your NumArray object will be instantiated and called as such:
// NumArray numArray(nums);
// numArray.sumRange(0, 1);
// numArray.sumRange(1, 2);
